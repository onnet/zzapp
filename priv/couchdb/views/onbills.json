{
    "_id": "_design/onbills",
    "language": "javascript",
    "views": {
        "crossbar_listing": {
            "map": "function(doc) { if (doc.pvt_type != 'onbill_doc' || doc.pvt_deleted) return; emit(doc._id, {id: doc._id, name: doc.name}); }"
        }
    }
}
